[project]
name = "alphafarm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pygame-ce>=2.5.7",
]

[dependency-groups]
build = [
    "pyinstaller>=6.0",
]
dev = [
    "pytest>=9.1.1",
]
